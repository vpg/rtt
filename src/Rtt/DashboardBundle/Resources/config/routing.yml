dashboard_homepage:
    path:     /hello
    defaults: { _controller: DashboardBundle:Default:index }

metrics_overview:
    path:     /metrics/
    defaults: { _controller:DashboardBundle:Metrics:overview}
metrics_topVisits:
    path:     /metrics/topVisits
    defaults: { _controller:DashboardBundle:Metrics:topVisits}
metrics_flop:
    path:     /metrics/flop
    defaults: { _controller:DashboardBundle:Metrics:flop}
metrics_search:
    path:     /metrics/search
    defaults: { _controller:DashboardBundle:Metrics:search}

metrics_list_by_type:
    path:     /metrics/listByType
    defaults: { _controller:DashboardBundle:Metrics:listByType}


metrics_insert:
    path:     /event/insert
    defaults: { _controller:DashboardBundle:Metrics:insertRecord}

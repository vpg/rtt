{% extends "DashboardBundle::layout.html.twig" %}


{% block head %}
    {{ parent() }}

    <script src="http://192.168.150.57:8086/socket.io/socket.io.js"></script>
    <script src="{{ asset('demo/rtt.js') }}" type="text/javascript"></script>
    <script type="text/javascript">
        $(function () {
            $(document).ready(function() {
                Highcharts.setOptions({
                    global: {
                        useUTC: false
                    }
                });

                var chart = new Highcharts.Chart({
                    chart: {
                        type: 'spline',
                        renderTo: 'chartContainer',
                        animation: Highcharts.svg, // don't animate in old IE
                        marginRight: 10,
                        events: {
                            load: function() {

                                // set up the updating of the chart each second
                                var series = this.series[0];
                                setInterval(function() {
                                    var x = (new Date()).getTime(), // current time
                                            y = Math.random();
                                    //series.addPoint([x, y], true, true);
                                }, 5000);
                            }
                        }
                    },
                    title: {
                        text: 'Visites temps réel'
                    },
                    xAxis: {
                        type: 'datetime',
                        tickPixelInterval: 150
                    },
                    yAxis: {
                        title: {
                            text: 'Value'
                        },
                        plotLines: [{
                                        value: 0,
                                        width: 1,
                                        color: '#808080'
                                    }]
                    },
                    tooltip: {
                        formatter: function() {
                            return '<b>'+ this.series.name +'</b><br/>'+
                                    Highcharts.dateFormat('%Y-%m-%d %H:%M:%S', this.x) +'<br/>'+
                                    Highcharts.numberFormat(this.y, 2);
                        }
                    },
                    legend: {
                        enabled: false
                    },
                    exporting: {
                        enabled: false
                    },
                    series: [
                        { name: 'Linux', data: [] },
                        { name: 'Windows', data: [] },
                        { name: 'Apple', data: [] },
                        { name: 'Other', data: [] }
                    ]
                });

                var vcount = 0;

                rtt.monitor( $('#offer_id').val(), function (event_x){
                    console.log('Monit %s', event_x.value);
                    vcount = vcount + event_x.value;
                    var y = vcount;
                    var x = (new Date()).getTime(); // current time
                    console.log(chart);
                    chart.series[0].addPoint({x : x, y : vcount}, false);
                    chart.redraw();
                });
            });


        });
    </script>

{% endblock %}


{% block content %}
    Offer id <input type="text" id="offer_id" name="offer_id" value="http://www.fr.frontvpg.frouvier.dev4-port.aix.vpg.lan/fiche-produit/details/32938"/>
    <input type='submit'>ok</button>

    <div id="chartContainer" style="min-width: 310px; height: 400px; margin: 0 auto"></div>
{% endblock %}
